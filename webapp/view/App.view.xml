<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns="sap.m" controllerName="companyRepo.appName.controller.App" displayBlock="true">
  <App>
    <Page title="{i18n>Header}">
      <content>
        <List headerText="Product Details" items="{/HeaderSet}">
          <items>
            <ObjectListItem title="{Docno}" number="{GrossAmount}" intro="{QuantityPerUnit}"/>
          </items>
        </List>
      </content>
    </Page>
  </App>
</mvc:View>
